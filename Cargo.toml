[package]
name = "lockedroom"
version = "0.1.0"
edition = "2021"

[dependencies]
axum = { version = "0.7.5", features = ["multipart"] }
tokio = { version = "1.10.0", features = ["full", "macros", "rt-multi-thread"] }
tower = "0.4.0"
